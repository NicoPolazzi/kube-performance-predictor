pipeline:
  sequence_length: 10
  train_ratio: 0.9
  split_strategy: extrapolation

model:
  hidden_size: 128
  hidden_size_2: 64

training:
  epochs: 200
  learning_rate: 0.001
  batch_size: 32
  weight_decay: 0.0005

scheduler:
  factor: 0.5
  patience: 40
  min_lr: 0.000001

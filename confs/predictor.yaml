pipeline:
  train_ratio: 0.9
  split_strategy: interpolation

model:
  hidden_size: 256
  hidden_size_2: 256
  head_hidden_size: 128
  dropout: 0.3

training:
  epochs: 100
  learning_rate: 0.003
  batch_size: 32
  weight_decay: 0.0001

scheduler:
  factor: 0.5
  patience: 20
  min_lr: 0.000001
